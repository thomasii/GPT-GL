{
  "dependencies": {
    "dat.gui": "^0.7.9",
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "parcel": "^2.8.3",
    "rimraf": "^5.0.0"
  },
  "scripts": {
    "clean": "rimraf .parcel-cache",
    "start": "npm run clean && parcel index.html --open",
    "build": "parcel build index.html"
  }
}
